{
  "max_attempts": 2,
  "max_total_tokens": 1,
  "routes": [
    {
      "name": "openai-compatible-image-primary",
      "base_url": "https://api.openai.com/v1",
      "api_key_env": "OPENAI_API_KEY",
      "models": ["gpt-image-1"],
      "priority": 10,
      "timeout_seconds": 180,
      "failure_threshold": 1,
      "cooldown_seconds": 300,
      "kind": "image"
    }
  ]
}
