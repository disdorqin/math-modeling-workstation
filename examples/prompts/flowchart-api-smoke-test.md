# Scientific Flowchart Generation Test

Create a publication-quality scientific workflow diagram for the paper **Attention Is All You Need**.

Show the method from left to right with five clearly separated stages:

1. Input token sequence and token embeddings.
2. Positional encoding added to embeddings.
3. Encoder stack with multi-head self-attention and position-wise feed-forward layers.
4. Decoder stack with masked self-attention, encoder-decoder attention, and feed-forward layers.
5. Linear projection, softmax, and predicted output sequence.

Visual requirements:

- clean white background;
- restrained academic palette: navy, teal, amber, and muted red;
- clear directional arrows and short labels;
- consistent typography and aligned modules;
- emphasize the repeated encoder and decoder blocks;
- include a small legend for attention, feed-forward, residual connection, and normalization;
- no decorative illustrations, no logos, no fake numerical data, no photorealistic elements;
- suitable as a Chinese mathematical-modeling workstation image-generation smoke test;
- output should look like a polished research-paper method figure, not a marketing poster.
